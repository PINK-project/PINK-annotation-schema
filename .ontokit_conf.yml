ONTOLOGY_NAME: pink_annotation_schema
ONTOLOGY_PREFIX: pink
ONTOLOGY_IRI: https://w3id.org/pink
GITHUB_REPOSITORY: PINK-project/PINK-annotation-schema
BUILD_DIR: build

# Optional: select subsections for the primary reference index.
# Default is "all".
# REFERENCE_SUBSECTIONS: all
# Example subset:
# REFERENCE_SUBSECTIONS: classes,annotation_properties,data_properties,object_properties,individuals
#
# Optional: additional reference indices for `ontokit docs`.
REFERENCE_INDICES:
  - ontology_file: build/pink-annotation-schema-squashed.ttl
    title: Properties Index
    docfile: properties.rst
    # iri_regex: https://example.org/other
    imported: false
    recursive: false
    subsections: annotation_proporties,object_properties,data_properties
